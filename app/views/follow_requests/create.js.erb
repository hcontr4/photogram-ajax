$(".<%= dom_id(@follow_request.sender) %>_<%= dom_id(@follow_request.recipient) %>_follow").html("<%= j(render partial: 'follow_requests/follow_unfollow', locals: { sender: current_user, recipient: @follow_request.recipient }) %>");
